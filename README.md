# 杂粮煎饼大王的硬件模块开源仓库
## 运放板

### 第一级作用
R16或者R9用作匹配（基本用不到，如果模块是50欧阻抗输出可以考虑使用）
c9为反馈电容，作用为：相位补偿，防止振荡，抑制高频噪声。
#### 跟随器
短接R6 ，In1或者In2输入。
R10和R15可直接短接，和R18相结合可以组成高通滤波用作滤除DAC直流量。
#### 同相加法器（偏置电路）
使用电阻为R10，R15，R5，R6
R4短接。
加法器输入端为In1和In2

$$
V_{out}=(1+\frac{R_6}{R_5})(V_{In1}\frac{R_{10}}{R_{15}+R_{10}}+V_{In2}\frac{R_{15}}{R_{15}+R_{10}})
$$

这里R都取同一值

$$
V_{out}=V_{In1}+V_{In2}
$$

如果加的一个是直流量就为偏置了，Stm32偏置1.65v就可以采集了。
#### 差分放大电路（减法器）
使用R5,R6,R10,R18
短接R3
输入为In1和In2

$$
V_{out}=\frac{R_{18}}{R_{10}+R_{18}}\frac{R_6+R_5}{R_5}V_{In2}-\frac{R_6}{R_5}V_{In1}
$$

这里取R6=R18，R5=R10

$$
V_{out}=\frac{R_6}{R_5}(V_{In2}-V_{In1})
$$

#### 同相放大
短接R15，R4.
输入为In1
使用R5，R6

$$
V_{out}=\frac{R_6}{R_5}V_{In1}
$$

这里使用In1和In2区别不大，替换一下对应电阻就好

#### 反向放大
短接R3，R5
使用R18，R17
$$
V_{out}=-\frac{R_{17}}{R_{18}}V_{In1}
$$
这里只能使用In2

#### 积分器
短接R18，R3
使用R5，C9（或者R6焊电容也行）

$$
V_{out}=-\frac{1}{R_5C_9}\int V_{In1}
$$

#### 微分器
短接R18，R3
使用R5，R6（R5焊电容）

$$
V_{out}=-R_5R_6\frac{dV_{In2}}{dt}
$$

#### 比较器
短接R3,R5,R10
In2输入信号，In1输入比较电平
运放开环增益非常大，所以一般会将信号放大到电源轨附件
这里并不推荐使用这种方式，一版的运放压摆率不是很大，在比较器方面性能不一定合适。
这里推荐专用芯片，如TLV3501等
##### 迟滞比较器
短接R3R5
使用R17，R18（也可以加上R10）
这里不做详细解释，可以参考模电来做设计


#### PS
目前是在设计时考虑到的，但是具体使用或许还能有更多拓展，可以自己考量

### 第二级作用
这里不做详细解释
一版用于做加法（偏置）或者跟随，或者同相放大
## 电源板
芯片使用LT78和LT79系列
电源从右往左为
- G +
